
import { registerAs } from '@nestjs/config';

const host = 'localhost'; // Change to your host
const port = 27017; // Change to your port
const user = 'admin'; // Better not change this
const password = '123456'; // Better not change this

export default { };

